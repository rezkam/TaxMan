package constants

const (
	// DateURLPattern is the pattern for the date in the URL.
	DateURLPattern = "date"
	// MunicipalityURLPattern is the pattern for the municipality name in the URL.
	MunicipalityURLPattern = "municipality"
)
